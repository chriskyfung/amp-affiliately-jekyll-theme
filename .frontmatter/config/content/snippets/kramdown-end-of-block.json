{
  "$schema": "https://frontmatter.codes/config/content.snippets.schema.json",
  "title": "End a Markdown Block with ^",
  "description": "Use the kramdown syntax to insert an End-Of-Block (EOB) marker, which is a ^ as the first character on an otherwise empty line, to end a kramdown block element",
  "body": "^"
}
